<button
  class="my-class phone-login-container"
  wx:if="{{ !$state.isLogin }}"
  open-type="getPhoneNumber"
  bindgetphonenumber="handleLogin"
>
  <slot></slot>
</button>
<block wx:if="{{ $state.isLogin }}">
  <slot></slot>
</block>
